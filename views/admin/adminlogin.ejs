<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Island+Moments&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@100;400;600;800&display=swap" rel="stylesheet">
    <title>Ark Vision - Admin Login</title>
</head>
<style>
    * {
        font-family: 'Poppins', sans-serif;
    }
    body {
        background: linear-gradient(24deg, rgba(0,0,0,1) 4%, rgb(170, 162, 10) 96%);
        height: 100vh;
    }
</style>
<body class="flex justify-center items-center">

    <div class="flex justify-center bg-white rounded-[20px] flex-col sm:flex-row ">
        <div>
            <img class="w-[400px] rounded-2xl" src="/assets/img/image 3.png" alt="Ark Vision Logo">
        </div>

        <form action="/ark/admin/login" method="POST" class="p-10 w-[350px] flex flex-col justify-center">
            <h1 class="text-[#000000] uppercase font-bold text-[20px] text-center p-2 bg-[#F6DE03] rounded-[10px]">Ark Visions</h1>
            <h1 class="mt-2 font-bold text-[25px]">Welcome Admin</h1>
            <p class="mt-3 text-[12px] font-semibold mb-[2px]">Please login to your account</p>
            <hr class="border-2 border-[#F6DE03]">

            <!-- Error message display -->
            <% if (typeof message !== 'undefined' && message) { %>
                <p class="mt-3 text-red-500 text-[12px] font-semibold"><%= message %></p>
            <% } %>

            <!-- Email input -->
            <div class="flex flex-col gap-1 mt-[8px]">
                <label for="email" class="text-[13px] font-bold">Email</label>
                <input type="email" name="email" value="<% try{%><%= email %><%} catch{} %>" class="border-[1px] border-[black] rounded-[10px] p-[2px]" required>
            </div>

            <!-- Password input -->
            <div class="flex flex-col gap-1 mt-1">
                <label for="password" class="text-[13px] font-bold">Password</label>
                <input type="password" name="password" class="border-[1px] border-[black] rounded-[10px] p-[2px]" required>
            </div>

            <!-- Remember me and Forgot Password -->
            <!-- <div class="flex justify-between items-center mt-3"> 
                <div class="flex items-center">
                    <input type="checkbox" name="remember">
                    <p class="ml-1 text-[12px]">Remember me</p>
                </div>
                <a href="/admin/forgot-password" class="text-[12px] font-bold underline text-[#F6DE03]">Forgot Password</a>
            </div> -->

            <!-- Login button -->
            <button type="submit" class="bg-[#F6DE03] mt-6 w-full rounded-[20px] py-[5px] px-[43%] self-center font-bold text-[14px]">
                Login
            </button>
            <a class="underline mt-2 w-full rounded-[20px] text-center py-[5px]  self-center font-bold text-[14px]" href="/ark/login">
                Back to Client Login
            </a>
        </form>
    </div>
    
</body>
</html>
